import { TobillionsPipe } from './tobillions.pipe';

describe('TobillionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TobillionsPipe();
    expect(pipe).toBeTruthy();
  });
});
